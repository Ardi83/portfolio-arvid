# Copy to .env to override defaults. Optional — the app works without it.

# Where the Blog link in the navbar points.
# Defaults to http://localhost:3000 in development (the blog's `yarn dev:api`)
# and to the production blog deployment in a build.
# VITE_BLOG_URL=http://localhost:3000
