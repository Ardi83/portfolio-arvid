# Copy to .env for local development.
#
# A plain SQLite file on disk. `yarn db:schema` creates it; it is gitignored.
# In Docker this is set in docker-compose.yml and points at the mounted volume.
DATABASE_URL=file:local.db
